import React from 'react'

function Firstchecktrue({arrlth}) {     // compelsory {} braces
    return (
        <>
        {arrlth.map((k)=>(
            <p>{k}</p>
        ))}
        </>
    )
}

export default Firstchecktrue
